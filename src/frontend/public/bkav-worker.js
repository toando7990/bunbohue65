/**
 * DELETED — Dead code removed.
 *
 * File này từng là bản copy frontend của BKAV invoice worker với IDL
 * PendingInvoiceItem cũ (13 trường, thiếu buyerName/buyerAddress/isRetailInvoice,
 * thừa customerName/customerPhone/customerAddress) — không khớp backend 15 trường.
 *
 * Đã xác nhận là dead code: không có file nào trong src/frontend/src
 * fetch/import/reference file này. Worker thật chạy trên VPS tại
 * bkav-worker/bkav-worker.js (đã được sửa IDL đúng 15 trường).
 *
 * Xóa file để tránh nhầm lẫn và đảm bảo không ai dùng IDL cũ.
 */
