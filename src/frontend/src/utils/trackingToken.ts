// This module was removed in v404 — order tracking no longer uses a per-order
// sessionStorage token. getOrderForTracking(orderId) is now anonymous and
// tokenless. This file is intentionally left empty; do not re-add token helpers.
export {};
