// Bank-code guard API mixin — REMOVED.
//
// The bankCode field and its BIN-validation guard have been removed from the
// business profile. This file is kept as an empty mixin so existing `include`
// statements in main.mo continue to resolve; it carries no state and no
// public surface.
mixin () {};
